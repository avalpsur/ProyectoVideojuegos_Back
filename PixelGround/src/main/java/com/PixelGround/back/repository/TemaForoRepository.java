package com.PixelGround.back.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.PixelGround.back.model.TemaForoModel;

public interface TemaForoRepository extends JpaRepository<TemaForoModel, Long> {}
